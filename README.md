# SourceGate
open-source Go-based security enforcement layer that sits in front of package managers like `npm` and `pip`. It intercepts package installation requests, analyzes risk signals in real time, explains findings, and then allows, warns, or blocks installation based on policy.
